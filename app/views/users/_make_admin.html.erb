<% if user.is_admin? %>
  <%= link_to 'Remove from admin', make_as_admin_user_path(user, type: 'remove'), class: 'btn btn-danger', remote: true  %>
<%else%>
  <%= link_to 'Make as admin', make_as_admin_user_path(user), class: 'btn btn-success', remote: true %>
<%end%>